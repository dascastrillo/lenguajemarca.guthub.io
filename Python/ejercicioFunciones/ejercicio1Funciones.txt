Ejemplos de funciones:
1. Realziar una funcion que imprima 'Hola Mundo'
    Como hacer las funciones:
    def <nombre de la funcion> ():
        print('Hola Mundo')

2. Realiza una funcion que sume dos numeros pasados por parámetros.
return= tiene que devolver algo.
    def sumaNumeros(numero1, numero2):
        return numero1 + 
        
3. Realiza una funcion que indique si un numero pasado por parametro es par o impar.
    def esPar(numero):
        return (numero % 2 == 0)

importar fichero dos tipos:
#  quiere decir que importe todo
from funciones import *
    sumaNumeros()

import funciones
    resultado = sumaNumeros(5, 10)#solo importamos el fichero
    print(resultado)

